# Claude Code Context: SNTO

SNTO means Smart Natural Tourism Observatory. The active case study is Parque Nacional de la Sierra de Guadarrama (PNSG). Sierra del Rincon is archived and is no longer the active pilot. The project is currently a Python 3.12 / Streamlit scientific-product prototype with research and SaaS ambitions.

## Current Status

- v1.0.0 → v2.0.0 are all released and tagged. **The `v2.0.0` tag + GitHub Release are LIVE** (published 2026-07-21). v2.0.0 consummates the roadmap's v2.0 milestone: modular architecture (Fase 4, shipped in v1.5.0) + persistent backend (Fase 5, shipped in v1.5.0) + role-based UI evolution (Fase 6, new in v2.0.0). Per the post-release flow, the dev marker was bumped afterward — `main` (`pyproject.toml` / `src/_version` / CITATION.cff) now reads **`2.1.0.dev0`**, meaning no *new* stable tag has shipped since v2.0.0 even though substantial work has landed on `main` (see below). Test suite: **954 passing, 1 skipped** (was 821 at the v2.0.0 tag).
- v1.2.0 (OAPN multi-park expansion), v1.3.0 (statistical rigor), and v1.4.0 (decision integration: risk brief #12, GIS export #25, evidence separation #10, positioning #9, field-validation tooling #26) are merged. Only the manual GEE field-validation campaign for the pilot parks remains open work, not a code blocker.
- **v1.5.0 consolidates Fase 4, #28, and Fase 5** into one tagged, published release. **Fase 4** (`app.py` modularization, #27), the **audience-views rescue** (#28), and **Fase 5** (v2.0 persistent-backend foundations, ADR-011) are all COMPLETE and in production — production Postgres cutover (ADR-011 §4bis) executed by the owner on 2026-07-18.
- **Fase 6 (v2.0 role-based UI evolution) is COMPLETE in code and shipped in v2.0.0.** All steps 6.0–6.7f are merged (PRs #75–#89): 4 IA layers (Decidir/Diagnosticar/Evidenciar/Gobernar) in `src/ui/navigation.py`, the 3 views absorbing the spec's 6 personas via `ViewProfile.home_layer`, asset-as-a-page routing (`src/ui/asset_detail.py`). Two-agent history: steps 6.1/6.3–6.6/6.7a–d by the owner's Codex agent (`codex/*` branches), 6.2a/b/6.7e/6.7f by Claude Code.
- **Post-v2.0 roadmap (`docs/roadmap/plan_v3_roadmap.md`, authored 2026-07-22) is executing.** v2.1 is **COMPLETE**; v2.2 is **complete in code but its three real-data feeds are not ingested** (see below — this is the single most misreported thing in this repo's docs); v2.5 is code-ready but gated on manual field work; v3.0 is **partially complete** in code (identity/tenancy, benchmarking, and two institutional-productization surfaces — CETS Fase I + PRUG monitoring — shipped; PostGIS geometry and a decoupled frontend not started). Detail:
  - **v2.1 — Activation & governance (COMPLETE):** deploy gated on CI-green (ADR-009, since #95) + rollback runbook; CI hardened (`--cov-fail-under=80`, report-only `mypy` on `src/persistence`/`src/api/v2`/`src/config`, a real-Postgres integration job); the curated territorial layer (`src/territorial/fixtures.py`) is now machine-tracked via `src/territorial/manifest.py`'s `TerritorialLayerManifest` (PR #97) instead of relying on docstring prose. Owner-executed items (Key Vault secrets migration, repo `git filter-repo` cleanup) remain open but are pure ops, documented in `docs/runbooks/`.
  - **v2.2 — Analytical depth (code COMPLETE; the three real feeds are NOT ingested):** `src/forecasting/` (trend + seasonal projection, uncertainty band, always `DataStatus.SIMULATED`, surfaced in Diagnosticar — PRs #99–#101) and the LAC/ROS carrying-capacity framework (`src/platform/lac_ros.py` + `pressure_capacity.py`, PR #103) are live — but LAC/ROS computes on the **curated** `visitor_capacity_annual` proxy (a planning range, never a measured limit), *not* on real mobility. **Three real-data paths were built, each with an honest availability gate that falls back to the previous labelled behaviour, and none has its data ingested yet** — verified by probing, not assumed:
    - **Mobility (PR #102):** the real MITMA zone crosswalk *is* committed (`src/mobility/reference/pnsg_mobility_zones.json`, 4 of 6 zones resolved) and `etl_mobility.py` exists, but `src/mobility/snapshot/mobility.json` has never been generated (the dir does not exist) → `mobility_snapshot_exists()` is `False`, so `tab_portfolio._real_municipal_pressure` returns `None` and capacity uses the curated estimate. When it does exist, the municipal figure attaches as **context only** — a municipal trip count is not trail footfall.
    - **SCM real zones:** `src/spatial_causality/zone_loader.py` has the `real_zones_exist()` gate, but `src/spatial_causality/zones/` does not exist → the α-decay simulated path is still what runs.
    - **SVI time-series:** `svi_history_available()` needs ≥2 dated snapshots; only `2026-06` ships → no real socioeconomic trend today.
    **Do not describe any of these three as "real data flowing".** The correct claim is that the ingestion path + gate shipped. `src/reporting/cets_readiness.py::resolve_signals` is the cheapest way to re-check all of it at once.
  - **v2.5 — Validation gate (code-ready, campaign PENDING):** the satellite↔field agreement runner (Spearman ρ / Cliff's δ, `src/validation/agreement.py`) and its status surface exist and are tested, but `clean_assets/field_validation/pnsg_field_observations_template.csv` still has **empty measurement columns**. **Do not claim satellite↔field validation until the real campaign (#26) runs** — this is the hard gate for v3.0's multi-park/transferability ambitions per ADR-003.
  - **v3.0 — Enterprise & network (PARTIAL):** identity & multi-tenancy foundation shipped — `Organization`/`User`/`UserRole` models, pure `authz.py` policy, `tenancy.py` provisioning service, nullable `Territory.org_id` (additive), and `authz_gate.py` enforced at all 5 `/api/v2` write endpoints (PRs #109, #110, #112, #113; ADR-002/005). **Dormant until real `User` rows exist** — today's single-shared-API-key model is unaffected. OAPN cross-park benchmarking roll-up shipped (`src/benchmarking/oapn_rollup.py`, PR #117, roadmap A15) — only rolls up parks with real committed trend data (pnsg, pn_monfrague, pn_tablas_daimiel), never the 13 unvalidated GEE templates. **Institutional productization STARTED:** `src/reporting/cets_readiness.py` (PR #123) builds a CETS Fase I readiness report — the CETS↔SNTO mapping is declared once and each requirement's evidence class is resolved from live repo probes, superseding the archived, v0.1.0-era prose in `docs/archive/CETS_Phase1_correspondence.md`. Two deliberately separate axes: `Coverage` (declared editorial position, data-independent) vs `DataStatus` (computed). `REAL` is the hard ceiling — it never emits a validation claim — and the out-of-scope CETS principles (4, 6, 7) are emitted rather than hidden. Surfaced as a third artifact in `tab_reports.py`. **PRUG monitoring shipped too:** `src/reporting/prug_monitoring.py` (PR #126) rolls up the 218 real trails' Sentinel-2 signal by official OAPN management zone (Uso Restringido/Moderado/Especial), surfacing the protection↔pressure mismatch via the existing `priority_index = (100−salud)×peso`. Real cartography × real signal ⇒ `EvidenceClass.REAL`, but framed as seasonal early-warning (two-scene ΔEHS), never a plan-compliance verdict, never field-validated (#26); degrades to `available=False` for territories without zonification (only PNSG has it). Fourth artifact in `tab_reports.py`. The OAPN dossier automation, PostGIS geometry, and a decoupled frontend are **not started**.
- **Mobile client (`mobile/`, separate Expo/TypeScript app, not part of the Python test count) — two phases, both COMPLETE:**
  - **Phase 1 (ADR-013, PR #116):** native shell (Expo Router) over synthetic fixtures only — no network calls.
  - **Phase 2 (ADR-014, PRs #118–#120):** a real `MobileHttpRepository` over `/api/v2`. Backend gap-fills shipped first: a `territories` router + `ManagedAssetOut` enrichment (dependency-free GeoJSON centroid, `src/api/v2/geometry.py`) in #118, a flat territory-scoped alerts listing in #119. The mobile app defaults to the mock repository; the real one activates only via `EXPO_PUBLIC_SNTO_USE_REMOTE_API=true`. Every mapped field degrades honestly to `null`/`'missing'` rather than fabricating (e.g. alerts carry no evidence-class field upstream, so `mapAlert` never assumes `'real'`).
- **Publication docs (PR #114, corrected by #115) are in place:** `docs/PUBLICATION_STRATEGY.md` (two-track: Pista A software artifacts vs Pista B scientific claims, frozen until #26), `docs/releases/v2.0.0.md`, an updated `docs/kit_difusion.md`. **Zenodo DOIs:** concept DOI `10.5281/zenodo.20818269`; canonical v2.0.0 DOI `10.5281/zenodo.21472647` (auto-published via the GitHub↔Zenodo webhook on tag push); a duplicate registration `10.5281/zenodo.21512233` was retired as a tombstone in PR #115 — **do not create another v2.0.0 version on Zenodo.**
- **README.md is synced through PR #121's follow-up** (v3.0 tenancy/identity, OAPN benchmarking, the mobile client, and the two-track publication policy are all in §1/§3/§12). **The whitepaper is deliberately NOT synced past v2.0.0** — its own §scope rule (line 9) states it tracks the latest *stable* methodological baseline and that `main` may hold later dev work. Do not "update" it with v2.1+ dev milestones; it advances only when a new stable tag ships, and its methodology is unchanged since the v1.3.0 statistical-rigor baseline.
- The operational roadmap is `docs/roadmap/plan_fases_post_v1.2.md`; the post-v2.0 execution plan is `docs/roadmap/plan_v3_roadmap.md`. `/api/v2` deployment stays gated behind ADR-012's trigger list (see "Next Recommended Actions" #4) — nothing here changes that.

## Pull Requests

- No SNTO release PRs are currently pending (repo has zero open PRs as of this writing). Fase 3 (#33–#37), Fase 4 modularization (#38–#53), the #28 audience-views rescue (#54–#59), Fase 5 v2.0 foundations (#61–#70), ADR-012 (#72), the v1.5.0 release (#73–#74), Fase 6 UI evolution (#75–#89), the post-v2.0 roadmap doc + v2.1 evidence-tracking (#97), v2.2 forecasting/mobility/LAC-ROS (#99–#103), v3.0 identity/tenancy (#109, #110, #112, #113), the publication-docs package (#114–#115), OAPN benchmarking (#117), and the mobile app Phases 1–2 (#116, #118–#120) are all merged. PR #85 (an earlier partial docs sync) was superseded by the Fase 6 closure sync and closed unmerged.
- Historical note: PR #1 (v1.1.0) merged; the statistical-rigor work formerly on `research/statistical-rigor` reached `main` via the v1.3.0 rescue.

## Architecture Facts

- `app.py` is composition/navigation only: page setup + sidebar assembly + the Fase 6 shell — an asset-as-a-page route check (`selected_asset_id` in `st.session_state` → `render_asset_page` + `st.stop()`), then nested `st.tabs()`: 4 IA layers (Decidir/Diagnosticar/Evidenciar/Gobernar, audience home layer first per `ViewProfile.home_layer`) × 14 modules (4/4/3/3) dispatched by `NavigationModule.key` to their `render_tab_x(...)` — + footer. The layer/module contract lives in `src/ui/navigation.py` and is enforced by `tests/ui/test_navigation.py`.
- Gobernar-layer surfaces added late in Fase 6: `tab_reports.py` (6.7e — downloadable executive brief over the decision portfolio via `src/reporting/territorial_brief.py` + real-trend GIS GeoJSON via `src/reporting/gis_export.py`; the two asset sets are distinct and labelled) and `tab_config.py` (6.7f — read-only territory registry, `src/platform/territory_registry.py`; nothing is marked field-validated).
- The UI lives in `src/ui/`: `layout.py` (page config, institutional CSS, territory registry, cached `load_dashboard`), `render_helpers.py` (presentation primitives), `render_widgets.py` (`st.`-rendering widgets), `src/ui/tabs/` (one module per tab), and `src/ui/services/` (persistence-backed query services, e.g. `urgent_actions.py`).
- Audience views: `src/platform/views.py` (`ViewProfile.section()` is the single layered-disclosure contract), `src/platform/telemetry.py` (local opt-in usage telemetry, `SNTO_TELEMETRY=1`). Financial figures are invariant across views (verified by `tests/integration/test_view_modulation.py`).
- **Persistence layer (Fase 5, ADR-011):** `src/persistence/` — SQLAlchemy 2.0 models (`models/`, now including `Organization`/`User` for v3.0 tenancy), typed repositories (`repositories/`), services (`services/`: the alert & field-verification bridges, lifecycle state machines, the audit choke-point, and `authz.py`/`tenancy.py` for v3.0), `session.py` (engine/session reading `settings.database_url`), and Alembic migrations (`migrations/`). `src/api/v2/` is the read+write surface over it; write endpoints are gated by `require_write_auth` (API key, `SNTO_API_KEY`) *and* by `authz_gate.py::authorize_territory_write` (dormant until real `User` rows exist), reads are open. Built against SQLite by default; Postgres is env-gated (`SNTO_DB_*`) and never auto-provisioned by code — production runs against Azure Postgres (`snto-db`) since the 2026-07-18 owner cutover. **The FastAPI app itself is still not deployed** (ADR-012 status quo, unchanged) — the deployed Streamlit Container App consumes persistence in-process (`src/ui/services/`), and the mobile app's real repository (Fase 2, ADR-014) is the first consumer designed to reach `/api/v2` over HTTP once it is deployed. A ready-to-run deploy runbook is `docs/runbooks/snto-api-deploy.md`.
- The analytical core under `src/` is well separated. The v2 API (`src/api/v2/`) is now the persistence-backed integration surface; the older stateless routers (`/evaluate_asset`, `/ranking`, `/alerts`) remain unchanged. New in v2.2: `src/forecasting/` (projection, always `SIMULATED`) and `src/mobility/` (real MITMA feed); new in v3.0: `src/benchmarking/oapn_rollup.py`.
- The Streamlit dashboard is feature-rich; the audience-view modulation (#28) reduces cognitive density per role.
- `mobile/` is a separate Expo/TypeScript client (own CI: `.github/workflows/mobile-ci.yml`, path-filtered on `mobile/**`, fully decoupled from Python CI). It is not the "decoupled frontend" trigger from ADR-012 §3 — that trigger refers to the *dashboard's* v2.0 UI splitting off, not this companion app.

## Product Direction

SNTO has the intellectual core of a reference product but the body of an advanced academic prototype. It should become a decision-intelligence layer for protected natural tourism destinations. It should not try to replace ArcGIS, Google Earth Engine, Sentinel Hub, Tableau, or Power BI. It should integrate with or sit above GIS, Earth observation, and BI tools.

## Roadmap Discipline

- v1.1: integration, stabilization, real temporal evidence, evidence clarity.
- v1.2: controlled expansion and pilot readiness.
- v2.0: modular architecture, UI evolution, backend/platform foundations.
- v3.0: enterprise/institutional maturity and multi-park scaling.

## Non-Negotiables

- Do not modify `main` directly.
- Do not merge PRs without explicit human approval.
- Do not mix documentation PRs with functional changes.
- Do not begin major UI evolution before modularizing `app.py`.
- Do not blur real, calibrated, synthetic, or simulated evidence.
- Do not overclaim scientific validity before validation.
- Preserve scientific transparency and methodological caveats.

## Next Recommended Actions

v2.0.0 is released and tagged; the v2.1/v2.2 dev milestones are complete in code on `main` (`2.1.0.dev0`, not yet its own tag). Remaining / next:

1. **Owner cleanup:** old merged branches (`feature/v1.5.0-*`, `release/v1.4.0`, and now the many merged `feat/v3.0-*`/`codex/*` branches from PRs #97–#120) can be deleted whenever convenient — none are referenced by anything live.
2. **Production DB cutover (ADR-011 §4bis): ✅ EXECUTED by the owner on 2026-07-18.** Azure Postgres Flexible Server `snto-db` (v16, Burstable B1ms, PostGIS, Sweden Central, same RG as the Container App), narrow firewall (Azure services + owner IP), Alembic created all 9 tables, and the five `SNTO_DB_*` secrets are wired into the `snto-observatory` Container App. `SNTO_API_KEY` intentionally left unset (writes open) for now — this stays safe only because the sole caller is the in-process Streamlit UI; it becomes non-negotiable to set *before* any exposure per #4. Ops gotcha: Container Apps in Single-revision mode do NOT pick up updated secret values without an explicit `az containerapp revision restart`.
3. **Field-validation campaign (#26) — the hard gate for v2.5/v3.0:** the tooling/protocol/agreement-runner are all merged; the real ground-truth campaign (penetrometer/cover/erosion on PNSG priority assets) is manual field work, still pending. Do not claim satellite↔field validation, and do not extend the OAPN benchmarking or multi-park transferability work, until this runs.
4. **Deploy the `/api/v2` HTTP surface:** scoped in **ADR-012** (`docs/decisions/ADR-012.md`), status **unchanged** — not deployed until a named external consumer appears, the #26 field-capture workflow needs to write from outside the dashboard, or the UI splits into a separate frontend. A ready-to-execute runbook already exists (`docs/runbooks/snto-api-deploy.md`, mirrors ADR-012 Option C exactly, sets `SNTO_API_KEY` atomically) — **do not write a new one**, just run it once a trigger fires. The mobile app's HTTP repository (Fase 2, ADR-014) is now a designed-but-unconnected consumer waiting for this.
5. **Fase 6: ✅ COMPLETE, shipped in v2.0.0.** The owner's visual pass and image rebuild that gated the v2.0.0 tag are done (release published 2026-07-21).
6. **Docs catch-up: ✅ DONE.** README.md now covers PRs #109–#120 (v3.0 tenancy/identity, OAPN benchmarking, the mobile client, the two-track publication policy). The whitepaper is intentionally left at its v2.0.0 stable baseline per its own scope rule — see "Current Status". Keep future docs syncs separate from functional changes per the non-negotiables.
7. **Ingest the three v2.2 real feeds (highest-value un-gated data work).** All three paths + gates are merged and each currently falls back to labelled curated/simulated behaviour, so ingesting any one of them upgrades real evidence with **zero code change**: (a) run `etl_mobility.py` to produce `src/mobility/snapshot/mobility.json`; (b) export the multi-scale GEE zones into `src/spatial_causality/zones/<asset_id>.json` to retire the α-decay simulation; (c) add a second dated snapshot under `src/socioeconomic/snapshot/history/municipalities_<YYYY-MM>.json` so SVI gains a real trend. Re-check with `resolve_signals()` and update the docs claim in the same breath. None of this needs the #26 field campaign.
8. **Owner-executed v2.1 ops (documented, not yet run):** Key Vault secrets migration (`docs/runbooks/keyvault-secrets.md`) and the `git filter-repo` history cleanup for the ~3–4 GB of committed `data/` noted in `DEPLOY.md`. Neither blocks anything else.
9. **Cutting the next release:** when the owner decides v2.1+v2.2 (or further) is ready to tag, bump `pyproject.toml` from `2.1.0.dev0`, run `scripts/sync_readme.py`, update `docs/releases/`, and tag — mirroring the v1.5.0→v2.0.0 flow. Nothing in v2.1/v2.2 changes any scientific claim, so this can happen independently of the #26 field campaign; a "v2.5" tag specifically, however, should not claim validation until #26 lands.

