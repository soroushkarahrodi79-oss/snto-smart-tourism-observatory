# ── Core scientific stack ────────────────────────────────────────────────────
numpy>=1.26
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
shapely>=2.0,<3.0

# ── Geospatial ───────────────────────────────────────────────────────────────
geopandas>=1.0,<2.0
rasterio>=1.3,<2.0
affine>=2.4,<3.0                # explicit pin (transitive via rasterio) — used in geometry.py
rasterstats>=0.19,<1.0

# ── STAC / Cloud-native EO (Task 1) ──────────────────────────────────────────
pystac-client>=0.8,<1.0        # STAC API search client
planetary-computer>=1.0,<2.0   # URL signing for Microsoft Planetary Computer (optional fallback)

# ── Database ─────────────────────────────────────────────────────────────────
psycopg2-binary>=2.9,<3.0
sqlalchemy>=2.0,<3.0
python-dotenv>=1.0,<2.0

# ── Web API ──────────────────────────────────────────────────────────────────
fastapi>=0.111,<1.0
uvicorn[standard]>=0.29,<1.0
httpx>=0.27,<1.0

# ── Dashboard ────────────────────────────────────────────────────────────────
streamlit>=1.35,<2.0
pydeck>=0.9,<1.0                 # Deck.gl WebGL map rendering (replaces Folium)
plotly>=5.20,<6.0                # Interactive charts (Portfolio Matrix, time series)
pandas>=2.1,<3.0                 # DataFrame layer for chart builders
# folium and streamlit-folium removed: replaced by pydeck for O(1) server-side RAM

# ── Dev / Test ───────────────────────────────────────────────────────────────
pytest>=8.2,<9.0
pytest-cov>=5.0,<6.0
ruff>=0.4,<0.5
